vagrant-node-grunt-bower
========================

Vagrant Box configuration for simple node.js development environment: node.js, npm, grunt, bower (by default, installed on a standard Ubuntu (x64)).
